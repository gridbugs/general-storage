[package]
name = "fib"
version = "0.1.0"
authors = ["Stephen Sherratt <stephen@sherra.tt>"]
edition = "2021"

[dependencies]
general_storage = { path = "../../../storage", features = ["yaml"] }
serde = { version = "1.0", features = ["serde_derive"] }
